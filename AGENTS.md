# 项目规则

## React 验证页面存放位置

- 以后所有用于验证、试验 React 技术的页面文件，一律放入项目根目录下的 `react-val-study/` 文件夹内，不要散落在根目录或其他位置。
- 新建此类文件时，如果 `react-val-study/` 不存在，先创建该文件夹再写入文件。
- **每写入一个 demo 后，必须在 `react-val-study/README.md` 的对照表里登记对应的 md 文档**：补充一行「demo 文件名 → 对应笔记（相对链接）→ 验证什么主题」；对应笔记在 `notes/` 里不存在时，先创建笔记再登记，保证每个 demo 都能顺着 README 找到讲解它的 md。

## JSX 代码示例必须写全，并解释逻辑

- 以后提供的所有 JSX / React 代码示例，一律写成**完整可运行**的形式：组件的定义、用到的 state 和变量、组件之间的父子层级关系都要写出来，不要只给节选片段。
- 片段缺上下文会让人看不懂「变量从哪来、谁是谁」——例如只写 `<Provider value={theme}>` 却不写 `theme` 来自祖先的 `useState`，就很难理解。
- 如果某些文档（如 notes/ 笔记）里确需节选，必须用注释标明省略了什么、变量来自哪里，或紧接着给出写全的版本。
- 每个 JSX / React 代码示例都必须**附带解释，把逻辑讲出来**：谁在什么时候执行、数据从哪里流到哪里、用户操作（点击/输入）之后发生了什么、为什么这样写。只给代码不讲逻辑，等于没讲。
- 解释形式不限，以下任一即可（拿不准就用第一种）：
  - **步骤拆解**：按 ①②③ 顺序讲清执行与数据流，例如「点击 → 调用回调 → setCount → 重渲染 → 界面更新」；
  - **逐行/逐块注释**：关键行标明「这行在干嘛、为什么需要它」；
  - **代码后紧跟一段「逻辑链路」文字说明**。
